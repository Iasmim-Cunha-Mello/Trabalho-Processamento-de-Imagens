# 🧠 Trabalho - Processamento de Imagens

Este projeto contém dois módulos principais:

- **`gemini.py`** → código utilizado para **testes e experimentos**.  
- **`api.py`** → implementação da **API principal** do projeto.

## ⚙️ Pré-requisitos

Antes de executar o projeto, certifique-se de ter uma **chave de API** configurada no seu ambiente.

Crie um arquivo `.env` na raiz do projeto e adicione sua chave:

```bash
API_KEY="sua_chave_aqui"

